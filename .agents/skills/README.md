# Skills del proyecto CECSA

Cada subcarpeta contiene un `SKILL.md` con instrucciones para agentes de IA (versionadas en git).

| Carpeta | Tema |
|---------|------|
| `bio_assistant/` | Chat, LangGraph, reservas 3 pasos, modal, FloatingCTA (+ `reference.md`) |
| `cal_com/` | Cal.com API: slots, presencial `278962`, admin agenda |
| `geo_maps/` | Leaflet + OSM + proxy Nominatim (`AddressPicker`) |
| `branding_manager/` | Marca y tokens CSS |
| `service_auditor/` | Especies / servicios |
| `copywriter_local/` | i18n y tono |
| `ui_ux_pro_max/` | UX, conversión, formulario reserva y admin |
| `tailwind_design_system/` | Tailwind v4 |

Índice ampliado en `AGENTS.md` en la raíz del repositorio.
