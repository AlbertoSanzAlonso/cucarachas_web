---
name: cal-com
description: >-
  DEPRECATED. Cal.com ya no se usa. Ver skill agenda/ (agenda propia Django + agenda-kit).
---

# Cal.com — DEPRECATED

**Sustituido por agenda propia.** Ver `.agents/skills/agenda/SKILL.md`.

Los módulos `cal_client.py` / `cal_booking.py` / `views/cal.py` quedan solo como referencia histórica; las rutas `/api/cal/*` y el webhook ya no están montados.
